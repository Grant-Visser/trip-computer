services:
  trip-computer:
    build: .
    ports:
      - "8080:80"
    volumes:
      - trip-computer-data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/data/trip-computer.db

volumes:
  trip-computer-data:
    driver: local
